# API backend (cổng 3001 trên VPS)
VITE_API_BASE=http://127.0.0.1:3001

# (tuỳ chọn) URL của web chính (cổng 3000) — dùng khi cần mở sang site
VITE_SITE_BASE=http://127.0.0.1:3000

# (tuỳ chọn) bật/đổi page size mặc định nếu app có dùng
VITE_DEFAULT_PAGE_SIZE=20
